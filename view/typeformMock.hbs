<!doctype html>
<!--suppress CssInvalidPropertyValue -->
<html lang="en">
<head>
    <meta charset="utf-8">
    <meta name="viewport"
          content="width=device-width, initial-scale=1, shrink-to-fit=no"
    >
    <title>{{form.title}} mock form &middot; IWGB</title>
    <link rel="stylesheet"
          href="https://cdn.jsdelivr.net/npm/bootstrap@4.5.3/dist/css/bootstrap.min.css"
    >
    <link rel="stylesheet"
          href="https://iwgbassets-f208.kxcdn.com/icons/css/all.css"
    >
    <style>
        .link-span-only:hover {
            text-decoration: none;
        }
        .link-span-only:hover span {
            text-decoration: underline;
        }
        .color-inherit {
            color: inherit;
        }
        .line-height-sm {
            line-height: 1.2;
        }
        *:disabled {
            cursor: not-allowed;
        }
        label p {
            margin-bottom: 0;
        }
    </style>
</head>
<body>
<div class="container">
    <div class="row my-5">
        <div class="col-sm-10 offset-sm-1 col-md-6 offset-md-3">
            <h1>QA mock form</h1>
            <div class="alert alert-secondary"
                 role="alert"
            >
                <p>As this is not a production environment, a non-production representation of the form has been generated below using the production Typeform configuration.</p>
                <p>Your AID, <code class="color-inherit">{{aid}}</code>, is only valid in the current non-production environment.</p>
                <a href="{{formUrl}}"
                   class="link-span-only"
                   target="_blank"
                   rel="noreferrer"
                >
                    <span class="mr-1">View on Typeform</span>
                    <i class="fas fa-external-link-alt"></i>
                </a>
            </div>
            <h2>{{form.title}}</h2>
            <form method="get"
                  action="{{dataUri}}"
            >
                <input type="hidden"
                       name="aid"
                       value="{{aid}}"
                >
                <input type="hidden"
                       name="form"
                       value="{{form.id}}"
                >
                <input type="hidden"
                       name="redirect"
                       value="{{completionUri}}"
                >
                {{#each form.fields}}
                    {{#with this}}
                        {{> field}}
                    {{/with}}
                {{/each}}
                {{#if dataUri}}
                    <div class="form-group">
                        <button type="submit"
                                class="btn btn-primary"
                        >
                            Continue
                        </button>
                    </div>
                {{else}}
                    <div class="form-group">
                        <button class="btn btn-primary mr-1"
                                disabled
                        >
                            Submit
                        </button>
                        <a class="btn btn-link"
                           href="{{completionUri}}"
                        >
                            Skip
                        </a>
                    </div>
                    <div class="form-group">
                        <p class="text-secondary line-height-sm">
                            <small>
                                This form sends data to production systems outside of this application, and so currently cannot be submitted in this environment.
                            </small>
                        </p>
                    </div>
                {{/if}}
            </form>
        </div>
    </div>
</div>
</body>
</html>